# Clone the repository
git clone <REPO_URL> cd Thesis

# Create and activate the virtual environment
uv venv source .venv/bin/activate

# Install dependencies
uv pip install requirements.txt

# Run the project
python main.py